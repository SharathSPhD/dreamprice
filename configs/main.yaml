defaults:
  - /world_model@world_model: mamba
  - /agent@agent: dreamer
  - /environment@environment: grocery
  - _self_

seed: 42
data_dir: docs/data
category: cso
n_steps: 100000
wandb_project: dreamprice
wandb_group: main
checkpoint_dir: checkpoints/
log_every: 100
save_every: 10000
