# @package _global_
defaults:
  - override /experiment: main

ablation_name: "no_stochastic_latent"
world_model:
  n_cat: 1
  n_cls: 1  # effectively deterministic
  z_dim: 1  # n_cat * n_cls
  kl_scale: 0.0
